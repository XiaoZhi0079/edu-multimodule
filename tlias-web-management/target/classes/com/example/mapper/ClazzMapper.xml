<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.ClazzMapper">

<!--    添加新班级-->
    <insert id="addClazz">
        insert into clazz(name, room, begin_date, end_date, master_id, subject, create_time, update_time) values(#{name}, #{room}, #{beginDate}, #{endDate}, #{masterId}, #{subject}, #{createTime}, #{updateTime})
    </insert>

<!--    更新班级信息-->
    <update id="updateClazz">
        UPDATE clazz
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="room != null and room != ''">
                room = #{room},
            </if>
            <if test="beginDate != null">
                begin_date = #{beginDate},
            </if>
            <if test="endDate != null">
                end_date = #{endDate},
            </if>
            <if test="masterId != null">
                master_id = #{masterId},
            </if>
            <if test="subject != null">
                subject = #{subject},
            </if>
            update_time = #{updateTime} -- 这个通常不放if里，每次更新都应该更新时间
        </set>
        WHERE id = #{id}
    </update>


<!--    根据id删除-->
    <delete id="deleteById">
        delete from clazz where id=#{id}
    </delete>


    <!--根据条件查询班级列表-->
    <select id="Page" resultType="com.example.pojo.Clazz">
        select
        c.*, e.name masterName,
        case when now()&gt;c.end_date then '已结课'
        when now()&lt;c.begin_date then '未开课'
        else '进行中' end as status
        from clazz c left join emp e on c.master_id=e.id
        <where>
            <if test="name != null and name != ''">
                and c.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="begin != null and end!= null">
                and c.end_date BETWEEN #{begin} AND #{end}
            </if>
        </where>
        ORDER BY
            create_time
    </select>

<!--    根据id查询-->
    <select id="findById" resultType="com.example.pojo.Clazz">
        select
            clazz.*, emp.name masterName,
            case when now()&gt;clazz.end_date then '已结课'
        when now()&lt;clazz.begin_date then '未开课'
        else '进行中' end as status from clazz left join emp on clazz.master_id=emp.id where clazz.id=#{id}
    </select>
</mapper>
